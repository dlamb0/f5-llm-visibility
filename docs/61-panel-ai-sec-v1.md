# Panel AI-SEC-v1 — frozen September 14, 2026

Category 11, AI Delivery & Security. Corpus doc 11 compiled/validated July 2026. English (US), unaided, no F5 names. 12 base prompts × (base + 2 paraphrases) = 36 variants. Frozen: any edit is v2 and breaks cross-cycle comparison.

The machine-readable copy is `harness/panel_ai_sec_v1.json`. This doc is the human-readable record.

| ID | Type | Base | Paraphrase a | Paraphrase b |
|---|---|---|---|---|
| B1 | problem-first | We're standing up an internal chatbot on top of an LLM and our security team won't let it go to production. What products or tools should we be looking at to secure it? | Our security team is blocking the launch of an internal LLM-based chatbot until it's properly secured. Which tools or products should we evaluate? | What products exist for securing an internal LLM chatbot so that a security review will sign off on putting it into production? |
| B2 | category-first | We're calling OpenAI and Anthropic from several internal apps and want one control point in front of all of them for security, PII redaction, and cost control. What are the leading options? | Several of our internal applications call OpenAI and Anthropic directly. What are the best options for putting a single gateway in front of all of them to handle security, PII redaction, and spend control? | Which products let us centralize security, PII scrubbing, and cost management for the LLM calls that multiple apps make to OpenAI and Anthropic? |
| B3 | problem-first | Someone on our team demoed a jailbreak of our customer-facing chatbot. What products detect and block prompt injection and jailbreak attempts at runtime? | Our chatbot got jailbroken in an internal demo. Which runtime tools catch prompt injection and jailbreaks before the model acts on them? | What are the options for detecting and stopping prompt-injection and jailbreak attacks against an LLM application in production? |
| B4 | persona-led | I'm a platform engineer at a bank. We run models from three providers plus some open-source ones on our own GPUs, with no consistent security policy across them, and compliance wants an audit trail of every AI interaction for the EU AI Act. What should we evaluate? | I work on the platform team at a bank. We have models from three vendors and some open-source ones running on our own GPUs, each with different security controls, and our compliance group needs a full audit trail of AI interactions for the EU AI Act. Which products should be on our shortlist? | As a platform engineer in financial services, what should I evaluate to enforce one consistent security policy across commercial and self-hosted LLMs and produce EU AI Act audit trails? |
| B5 | constraint-led | What's the best AI gateway for Kubernetes that can front both self-hosted Llama models and OpenAI, with rate limiting, caching, and PII redaction, and can run on-prem or air-gapped? | Which AI gateway for Kubernetes can sit in front of self-hosted Llama and OpenAI with rate limiting, caching, and PII redaction, and be deployed on-prem or in an air-gapped environment? | We need a Kubernetes-native LLM gateway that handles both OpenAI and local Llama backends, does rate limiting, response caching, and PII redaction, and works air-gapped. What are the best choices? |
| B6 | problem-first | We can't let customer PII or source code end up in prompts sent to an external model, and compliance needs proof it was redacted before it left. What tools do this inline? | How do we stop customer PII and proprietary source code from being sent to external LLMs in prompts, with evidence for compliance that redaction happened before egress? | What inline tools redact sensitive data like PII and source code from prompts before they reach an external model, and log it for auditors? |
| B7 | category-first | We don't have a full-time AI red team. What automated tools exist for adversarial testing of LLM applications and agents before they go to production? | Our team has no dedicated AI red team. Which automated tools can run adversarial tests against LLM apps and agents before production? | What automated red-teaming products exist for stress-testing LLM applications and AI agents for vulnerabilities before launch? |
| B8 | constraint-led | We need an independent security benchmark to justify our choice of LLM provider to our risk committee. Are there published rankings of model security, and what vendors offer testing against our own deployment? | Our risk committee wants an independent basis for choosing an LLM provider on security grounds. Are there published security rankings of models, and which vendors can test our specific deployment? | Is there an independent benchmark ranking LLMs by security, and which companies offer adversarial testing against a customer's own model deployment? |
| B9 | problem-first | Developers are spinning up AI agents and MCP tools and we can't see what they're actually doing or which ones are sanctioned. What gives us visibility into agent traffic? | Our developers keep launching AI agents and MCP servers and we have no visibility into what they're doing or which are approved. What tooling shows us agent traffic? | How can we get visibility into AI agent and MCP traffic in our environment, including distinguishing sanctioned agents from shadow ones? |
| B10 | problem-first | Employees are pasting customer data into ChatGPT. We blocked it and people just work around the block. What products let us give staff a sanctioned AI portal with data redaction and an audit trail instead? | Staff are pasting customer records into ChatGPT, and blocking it just pushed them to workarounds. Which products let us offer an approved AI portal with redaction and audit logging? | Instead of blocking public AI tools that employees route around anyway, what products give the workforce a sanctioned GenAI front door with sensitive-data redaction and audit trails? |
| B11 | constraint-led | We have no idea how many AI tools our employees are using. What detects shadow AI on the corporate network, agentless on managed devices, and plugs into our existing SASE, DLP, and SIEM rather than replacing them? | We need to discover all the AI tools employees are using, without installing agents on managed devices, and feed that into our SASE, DLP, and SIEM. What products do this? | Which shadow-AI discovery products work agentlessly at the network level and integrate with existing SASE, DLP, and SIEM stacks rather than replacing them? |
| B12 | comparison | Compare the leading enterprise AI security platforms for a CISO who wants governance, discovery, security testing, and runtime protection in one place rather than five point tools. | For a CISO consolidating AI security, which platforms combine AI governance, discovery, testing, and runtime protection, and how do they compare? | What are the top enterprise AI security platforms that cover discovery, red-teaming, runtime guardrails, and governance in a single product, and how do they stack up? |

## Competitor set used by the scorer

Confirmed (in corpus): HiddenLayer, Lakera (Check Point), Protect AI (Palo Alto / Prisma AIRS), Robust Intelligence (Cisco AI Defense), Prompt Security (SentinelOne), Cloudflare, Kong. Acquired names are scored under both standalone and acquirer names; standalone-only mentions are reported as a competitor stale-name rate.

Unconfirmed additions (awaiting Daniel's sign-off; excluded from `share_of_model_confirmed_set`): Zscaler, Netskope, Portkey, LiteLLM, NVIDIA NeMo Guardrails, AWS Bedrock Guardrails, Azure AI Content Safety, Google Model Armor.

Watchlist (counted, not in denominator): ~30 additional names in `harness/aliases_ai_sec.py`; anything above 5% of responses is flagged for promotion next cycle.

## Engines (decision Sept 14)

Sonnet tier from each provider: Claude Sonnet, OpenAI mid tier, Gemini Flash. Exact pinned IDs to be recorded in the report header from `--list-models`.
