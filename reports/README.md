# reports/

One folder per cycle. Each contains the Section 4 report (`report.md`), the leadership front sheet (`front-sheet.md`), and the scorer outputs the report was built from (`scored/`). Written by Claude from the matching `results/` folder.
